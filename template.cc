#include <bits/stdc++.h>
using namespace std;

using ll = int64_t;

const ll INF = 1e9 + 7;
const ll mod = 1e9 + 7;

int main () {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--) {

    }
    return 0;
}